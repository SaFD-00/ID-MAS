# 케이스 분류 및 파이프라인 로그 형식 수정 계획

## 개요

현재 2개 케이스(A, B) 분류를 3개 케이스(A, B, C)로 세분화하고, 관련 로그 형식을 수정합니다.

## 현재 상태

### 케이스 분류
| Case | 조건 | 설명 |
|------|------|------|
| A | PO 충족 | 초기 또는 다중 시도 응답 (1~5회차 성공) |
| B | max_iterations 후 PO 미충족 | teacher가 재구성 |

### 관련 파일
- `learning_loop/graph/state.py`: SFTCase enum 정의, 상태 스키마
- `learning_loop/graph/nodes.py`: 케이스 분류 로직
- `learning_loop/graph/graph.py`: 통계 출력 로직
- `ARCHITECTURE.md`: 문서

## 변경 계획

### 새로운 케이스 분류
| Case | 조건 | 설명 |
|------|------|------|
| **A** | 1회차에 PO 충족 | 한번에 성공 |
| **B** | 2~5회차에 PO 충족 | Iterative Scaffolding 성공 |
| **C** | 5회 후 PO 미충족 | 재구성 필요 |

### 성공 기준
- PO 충족만 확인 (현재 방식 유지)
- `all_satisfied=True` 조건으로 성공 판정

### SFT 데이터 처리
- **Case A**: 학생의 1회차 응답 그대로 사용
- **Case B**: teacher model이 scaffolding 과정을 요약하여 재구성 (AI agent 활용)
- **Case C**: teacher model이 정답 기반으로 재구성 (현재 로직 유지)

## 구현 단계

### Step 1: SFTCase enum 수정 (`state.py`)

**파일**: `learning_loop/graph/state.py:21-24`

```python
# 변경 전
class SFTCase(str, Enum):
    A = "A"  # PO satisfied (initial or multi-attempt response)
    B = "B"  # PO not satisfied after max iterations (reconstructed by teacher)

# 변경 후
class SFTCase(str, Enum):
    A = "A"  # PO satisfied on first attempt (한번에 성공)
    B = "B"  # PO satisfied via iterative scaffolding (2-5회차 성공)
    C = "C"  # PO not satisfied after max iterations (재구성 필요)
```

### Step 2: 상태 카운터 추가 (`state.py`)

**파일**: `learning_loop/graph/state.py:114-116`

현재:
```python
first_attempt_correct: int
multi_attempt_correct: int
failed_reconstructed: int
```

변경 후:
```python
case_a_count: int       # 1회차 성공 (한번에 성공)
case_b_count: int       # 2~5회차 성공 (Iterative Scaffolding 성공)
case_c_count: int       # 5회 실패 후 재구성
```

### Step 3: create_initial_state 수정 (`state.py`)

**파일**: `learning_loop/graph/state.py:135-214`

```python
# 기존 카운터 변경
first_attempt_correct=0,     →  case_a_count=0,
multi_attempt_correct=0,     →  case_b_count=0,
failed_reconstructed=0,      →  case_c_count=0,
```

### Step 4: get_statistics 수정 (`state.py`)

**파일**: `learning_loop/graph/state.py:217-240`

```python
def get_statistics(state: IDMASState) -> Dict[str, Any]:
    case_a = state.get("case_a_count", 0)
    case_b = state.get("case_b_count", 0)
    case_c = state.get("case_c_count", 0)

    return {
        "total_questions": state.get("total_questions", 0),
        "scaffolding_processed": state.get("scaffolding_processed", 0),
        "case_statistics": {
            "case_a": case_a,      # 한번에 성공
            "case_b": case_b,      # Iterative Scaffolding 성공
            "case_c": case_c,      # 5회 실패 후 재구성
            "success_total": case_a + case_b,
            "success_rate": (case_a + case_b) / state.get("scaffolding_processed", 1),
        },
        "sft_data_breakdown": {
            "case_a": case_a,
            "case_b": case_b,
            "case_c": case_c,
        },
    }
```

### Step 5: 케이스 분류 로직 수정 (`nodes.py`)

**파일**: `learning_loop/graph/nodes.py:86-103`

변경 전:
```python
if result.get("scaffolding_correct"):
    updates["scaffolding_correct_count"] = state.get("scaffolding_correct_count", 0) + 1
    print(f"  -> Correct! (Case A)")
    if use_iterative:
        scaffolding_info = result.get("iterative_scaffolding", {})
        iterations_needed = scaffolding_info.get("iterations_needed", 1)
        if iterations_needed == 1:
            updates["first_attempt_correct"] = state.get("first_attempt_correct", 0) + 1
        else:
            updates["multi_attempt_correct"] = state.get("multi_attempt_correct", 0) + 1
else:
    print(f"  -> Incorrect (predicted: {result.get('predicted_answer')})")
    if result.get("sft_case") == "B":
        updates["failed_reconstructed"] = state.get("failed_reconstructed", 0) + 1
```

변경 후:
```python
sft_case = result.get("sft_case")
if sft_case == SFTCase.A.value:
    updates["case_a_count"] = state.get("case_a_count", 0) + 1
    print(f"  -> Case A: 한번에 성공! (1회차 PO 충족)")
elif sft_case == SFTCase.B.value:
    updates["case_b_count"] = state.get("case_b_count", 0) + 1
    iterations = result.get("iterative_scaffolding", {}).get("iterations_needed", 0)
    print(f"  -> Case B: Iterative Scaffolding 성공! ({iterations}회차 PO 충족)")
elif sft_case == SFTCase.C.value:
    updates["case_c_count"] = state.get("case_c_count", 0) + 1
    print(f"  -> Case C: 5회 실패 후 재구성")
```

### Step 6: Case B 재구성 함수 추가 (`teacher_model.py`)

**파일**: `learning_loop/teacher_model.py`

Case B (Iterative Scaffolding 성공)에서 scaffolding 과정을 요약하여 깔끔한 SFT 응답으로 재구성하는 함수 추가:

```python
def reconstruct_successful_scaffolding(
    self,
    problem_text: str,
    ground_truth: str,
    task_analysis: str,
    conversation_history: List[Dict],
    final_response: str,
    iterations_needed: int
) -> Dict[str, Any]:
    """
    Case B: Iterative Scaffolding 성공 후 SFT 데이터용 응답 재구성

    학생이 2~5회차에 성공한 경우:
    1. scaffolding 과정에서 얻은 핵심 학습 포인트 추출
    2. 최종 성공 응답을 기반으로 깔끔하게 재구성
    3. teacher guidance의 핵심을 자연스럽게 통합

    Args:
        problem_text: 문제 텍스트
        ground_truth: 정답
        task_analysis: 과제 분석 결과
        conversation_history: 전체 대화 기록
        final_response: 최종 성공한 학생 응답
        iterations_needed: 성공까지 걸린 iteration 수

    Returns:
        {
            "reconstructed_response": str,  # SFT 데이터로 사용할 재구성된 응답
            "key_learning_points": List[str],  # scaffolding에서 얻은 핵심 학습 포인트
            "improvement_summary": str  # 학생이 어떻게 개선했는지 요약
        }
    """
```

**프롬프트 추가 (`prompts/learning_prompts.py`)**:

```python
SUCCESSFUL_SCAFFOLDING_RECONSTRUCTION_PROMPT = """
You are an expert teacher reconstructing a successful learning outcome into clean SFT training data.

## Problem
{problem_text}

## Correct Answer
{ground_truth}

## Task Analysis
{task_analysis}

## Scaffolding Process Summary
The student succeeded after {iterations_needed} iterations.
{conversation_summary}

## Final Successful Response
{final_response}

## Your Task
Reconstruct the student's learning journey into a single, clean response that:
1. Incorporates the key insights gained through scaffolding
2. Presents a clear, step-by-step solution
3. Naturally integrates the guidance that led to success
4. Is suitable for SFT training (no explicit mention of scaffolding process)

Return JSON:
{{
    "reconstructed_response": "A clean, comprehensive solution...",
    "key_learning_points": ["Point 1", "Point 2", ...],
    "improvement_summary": "How the student improved..."
}}
"""
```

### Step 7: _process_iterative_scaffolding 수정 (`nodes.py`)

**파일**: `learning_loop/graph/nodes.py:243-303`

성공 시 (PO 충족) - Case A/B 분기 및 Case B 재구성:
```python
if all_satisfied:
    iterations_needed = len(iterations)

    if iterations_needed == 1:
        # Case A: 1회차 성공 - 학생 응답 그대로 사용
        sft_output = response
        sft_case = SFTCase.A.value
    else:
        # Case B: 2~5회차 성공 - teacher가 scaffolding 과정 재구성
        reconstruction = teacher_model.reconstruct_successful_scaffolding(
            problem_text=question["problem_text"],
            ground_truth=question["output"],
            task_analysis=task_analysis,
            conversation_history=conversation_history,
            final_response=response,
            iterations_needed=iterations_needed,
        )
        sft_output = reconstruction.get("reconstructed_response", response)
        sft_case = SFTCase.B.value

    return QuestionResult(
        ...
        sft_case=sft_case,
        sft_response=sft_output,
        ...
    )
```

실패 시 (재구성):
```python
# 변경 전
sft_case=SFTCase.B.value,

# 변경 후
sft_case=SFTCase.C.value,
```

### Step 8: 체크포인트 로딩 수정 (`state.py`)

**파일**: `learning_loop/graph/state.py:243-300`

`load_checkpoint_from_logs` 함수에서 새로운 카운터 지원:
```python
checkpoint_data = {
    "scaffolding_results": [],
    "scaffolding_processed": 0,
    "case_a_count": 0,
    "case_b_count": 0,
    "case_c_count": 0,
}

# 결과 처리 시
sft_case = result.get("sft_case")
if sft_case == "A":
    checkpoint_data["case_a_count"] += 1
elif sft_case == "B":
    checkpoint_data["case_b_count"] += 1
elif sft_case == "C":
    checkpoint_data["case_c_count"] += 1
```

### Step 9: 파이프라인 출력 수정 (`graph.py`)

**파일**: `learning_loop/graph/graph.py:317-329`

변경 전:
```python
print(f"Scaffolding Correct (Case A): {stats['scaffolding_correct']}")
print(f"Failed Reconstructed (Case B): {stats['sft_case_a_failed']}")
if stats.get('iterative_scaffolding'):
    iter_stats = stats['iterative_scaffolding']
    print(f"  - First Attempt Correct: {iter_stats['first_attempt_correct']}")
    print(f"  - Multi Attempt Correct: {iter_stats['multi_attempt_correct']}")
```

변경 후:
```python
case_stats = stats.get('case_statistics', {})
print(f"\n[Case Statistics]")
print(f"  Case A (한번에 성공): {case_stats.get('case_a', 0)}")
print(f"  Case B (Iterative Scaffolding 성공): {case_stats.get('case_b', 0)}")
print(f"  Case C (5회 실패 후 재구성): {case_stats.get('case_c', 0)}")
print(f"  ────────────────────────────")
print(f"  Success Total (A+B): {case_stats.get('success_total', 0)}")
print(f"  Success Rate: {case_stats.get('success_rate', 0) * 100:.1f}%")
```

### Step 10: SFT 데이터 생성 로직 확인 (`nodes.py`)

**파일**: `learning_loop/graph/nodes.py:361-419`

Case C 지원 추가:
```python
# 변경 전
if result.get("sft_case") in (SFTCase.A.value, SFTCase.B.value):

# 변경 후
if result.get("sft_case") in (SFTCase.A.value, SFTCase.B.value, SFTCase.C.value):
```

instruction 메시지 업데이트:
```python
if case == SFTCase.A.value:
    instruction = "Solve the following problem."
elif case == SFTCase.B.value:
    instruction = "Solve the following problem with teacher guidance."
elif case == SFTCase.C.value:
    instruction = "Solve the following problem, learning from common mistakes."
```

### Step 11: ARCHITECTURE.md 문서 업데이트

케이스 분류 테이블 및 관련 설명 업데이트:
- SFT 데이터 생성 섹션
- Iterative Scaffolding 프로세스 섹션
- 로그 출력 예시

## 영향 범위

### 수정 파일
1. `learning_loop/graph/state.py` - SFTCase enum, 상태 카운터, 통계 함수
2. `learning_loop/graph/nodes.py` - 케이스 분류 로직, SFT 생성
3. `learning_loop/graph/graph.py` - 통계 출력
4. `learning_loop/teacher_model.py` - Case B 재구성 함수 추가
5. `prompts/learning_prompts.py` - Case B 재구성 프롬프트 추가
6. `ARCHITECTURE.md` - 문서

### 기존 데이터 호환성
- 기존 로그 파일의 `sft_case: "B"` (재구성)는 `sft_case: "C"`로 해석되어야 함
- `load_checkpoint_from_logs`에서 backward compatibility 처리 필요:
  ```python
  # Legacy "B" case (재구성) → 새로운 "C" case로 매핑
  if sft_case == "B" and not result.get("scaffolding_correct"):
      sft_case = "C"
  ```

## 테스트 계획

1. 단위 테스트: 각 케이스별 분류 로직 검증
2. 통합 테스트: 전체 파이프라인 실행 후 로그 형식 확인
3. 회귀 테스트: 기존 체크포인트 로딩 호환성 확인

## 예상 결과 로그

```
[Scaffolding] Processing: gsm8k_001
  -> Case A: 한번에 성공! (1회차 PO 충족)

[Scaffolding] Processing: gsm8k_002
    -> PO not satisfied on iteration 1. Continuing scaffolding...
    -> Success on iteration 3! (PO satisfied: True)
  -> Case B: Iterative Scaffolding 성공! (3회차 PO 충족)

[Scaffolding] Processing: gsm8k_003
    -> PO not satisfied on iteration 1. Continuing scaffolding...
    -> PO not satisfied on iteration 2. Continuing scaffolding...
    -> PO not satisfied on iteration 3. Continuing scaffolding...
    -> PO not satisfied on iteration 4. Continuing scaffolding...
    -> PO not satisfied on iteration 5. Continuing scaffolding...
    -> Failed after 5 iterations. (Reason: po_not_satisfied) Reconstructing...
  -> Case C: 5회 실패 후 재구성

============================================================
PIPELINE COMPLETE
============================================================
Total Questions: 100
Scaffolding Processed: 100

[Case Statistics]
  Case A (한번에 성공): 45
  Case B (Iterative Scaffolding 성공): 35
  Case C (5회 실패 후 재구성): 20
  ────────────────────────────────
  Success Total (A+B): 80
  Success Rate: 80.0%

SFT Data Generated: 100
```

## 완료 기준

- [ ] SFTCase enum이 A, B, C 3개로 변경됨
- [ ] 케이스 분류가 iteration 횟수에 따라 정확히 동작
- [ ] Case B 재구성 함수(`reconstruct_successful_scaffolding`) 구현됨
- [ ] Case B SFT 데이터가 teacher model에 의해 재구성됨
- [ ] 통계 출력이 새로운 형식으로 표시
- [ ] 기존 체크포인트 파일과 backward compatibility 유지
- [ ] ARCHITECTURE.md 문서가 업데이트됨
